server:
    port: 8080

spring:
    datasource:
        url: jdbc:mysql://localhost:3306/studentmanagement?useSSL=false&serverTimezone=UTC
        username: root
        'password': Aa@123123
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                format_sql: true
                use_sql_comments: true

